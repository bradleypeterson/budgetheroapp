<Page
    x:Class="DesktopApplication.Views.Forms.AccountForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DesktopApplication.Views.Forms"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <!-- Content body -->
        <TextBox Header="Account Name" Margin="0,10,0,10" PlaceholderText="Account Name" Foreground="{StaticResource PrimaryForegroundBrush}"/>
        <TextBox Header="Account Balance" Margin="0,10,0,10" PlaceholderText="Account Balance" Foreground="{StaticResource PrimaryForegroundBrush}"/>
        <DropDownButton Content="Account Type" Margin="0,10,0,10">
            <DropDownButton.Flyout>
                <MenuFlyout Placement="Bottom">
                    <MenuFlyoutItem Text="Checking"/>
                    <MenuFlyoutItem Text="Savings"/>
                </MenuFlyout>
            </DropDownButton.Flyout>
        </DropDownButton>
    </StackPanel>
    
</Page>
