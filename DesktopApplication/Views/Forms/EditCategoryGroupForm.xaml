<Page
    x:Class="DesktopApplication.Views.Forms.EditCategoryGroupForm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DesktopApplication.Views.Forms"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        
        <ComboBox Grid.Row="0" x:Name="EditCombo" Header="Group Name" PlaceholderText="Choose Group To Edit" SelectionChanged="EditCombo_SelectionChanged">
            <x:String>Housing</x:String>
            <x:String>Utilities</x:String>
            <x:String>Food</x:String>
            <x:String>Transportation</x:String>
        </ComboBox>

        <TextBox x:Name="GroupNameText" Grid.Row="1" Header="Group Name" Margin="0,10,0,10"/>


            <RadioButtons Grid.Row="2" Header="Options:">
            <RadioButtons.Style>
                <Style TargetType="RadioButtons">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="RadioButtons">
                                <StackPanel>

                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Normal" />

                                            <VisualState x:Name="Disabled">
                                                <VisualState.Setters>
                                                    <Setter Target="HeaderContentPresenter.Foreground" Value="{ThemeResource RadioButtonsHeaderForegroundDisabled}" />

                                                </VisualState.Setters>
                                            </VisualState>

                                        </VisualStateGroup>

                                    </VisualStateManager.VisualStateGroups>
                                    <ContentPresenter x:Name="HeaderContentPresenter" Content="{TemplateBinding Header}" ContentTemplate="{TemplateBinding HeaderTemplate}" Foreground="{ThemeResource RadioButtonsHeaderForeground}" Margin="{ThemeResource RadioButtonsTopHeaderMargin}" />
                                    <ItemsRepeater x:Name="InnerRepeater">
                                        <ItemsRepeater.Layout>
                                            <StackLayout  Spacing="15" Orientation="Horizontal"/>
                                        </ItemsRepeater.Layout>
                                    </ItemsRepeater>
                                </StackPanel>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </RadioButtons.Style>
                <RadioButton Content="Add Category Item" Checked="RadioButton_Checked"/>
                <RadioButton Content="Remove Category Item" Checked="RadioButton_Checked"/>
            </RadioButtons>

        <StackPanel x:Name="AddItemPanel" Grid.Row="3" Visibility="Collapsed">
            <TextBlock Text="ADD ITEM"/>
            <!-- TODO: Add Text Box to enter name of category to add-->
            <!-- TODO: Add Text Box to enter Budget of category to add-->
        </StackPanel>

        <StackPanel x:Name="RemoveItemPanel" Grid.Row="3" Visibility="Collapsed">
            <TextBlock Text="REMOVE ITEM"/>
            <!-- TODO: Add combo box showing items available to get rid of-->
        </StackPanel>
        
    </Grid>


</Page>
